# API base URL (without /api/v1 — the client appends it).
EXPO_PUBLIC_API_URL=https://your-api.onrender.com

# Sentry (optional).
EXPO_PUBLIC_SENTRY_DSN=

# Filled by `eas init`.
EXPO_PUBLIC_EAS_PROJECT_ID=
